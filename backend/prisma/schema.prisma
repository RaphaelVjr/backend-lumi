
generator client {
  provider = "prisma-client-js"
}


datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Fatura {
  id        Int      @id @default(autoincrement())
  cliente   String
  mesReferencia String
  energiaEletrica  Json
  energiaSCEEE     Json
  energiaCompensada Json
  contribIlumPublica Json
}

